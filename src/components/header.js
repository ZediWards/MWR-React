import * as React from "react"

const Header = () => (
  <header
    style={{
      background: `grey`,
      marginBottom: `1.45rem`
    }}
  >
    This is the header
  </header>
)

export default Header
